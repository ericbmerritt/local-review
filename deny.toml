[advisories]
ignore = []

[licenses]
allow = [
  "Apache-2.0",
  "MIT",
  "MIT-0",
  "BSD-2-Clause",
  "Unicode-3.0",
  "Unlicense",
  "Zlib",
]
confidence-threshold = 0.8
unused-allowed-license = "deny"

[bans]
multiple-versions = "deny"
wildcards = "deny"
highlight = "all"
skip = [
  { crate = "wit-bindgen@0.51.0", reason = "two wit-bindgen versions reach us via tempfile's wasip2/wasip3 deps; cannot collapse without forking. Pinned so the skip fails when 0.51 drops out and we can re-evaluate." },
  { crate = "hashbrown@0.16.1", reason = "ratatui 0.30.x pulls in two hashbrown versions transitively: kasuari (its constraint solver) uses 0.16, lru uses 0.17. Cannot collapse without upstream coordination. Pinned so the skip fails when 0.16 drops out and we can re-evaluate." },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
